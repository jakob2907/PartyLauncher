package games.SuperMario;

import javafx.scene.Scene;
import states.BasicGame;
import states.Game;
import states.StateHandler;

public class SuperMario extends BasicGame {

    public SuperMario(StateHandler sh)
    {
        super(sh);
    }

}
